# go
/bin/
/tera
*.test
*.out

# sqlite — the database is the business's data, never the repo's
*.db
*.db-wal
*.db-shm

# backups (R14) — local target, not source
/backups/

# node / vite
node_modules/
web/dist/

# env
.env
.env.*
!.env.example

# editors / os
.idea/
.vscode/
*.swp
.DS_Store
